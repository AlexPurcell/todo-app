</body>

</html>
<h3>

    <ul>
        <% for(var todos of items) { %>
            <li>
                <%= todos.title %> <input type="checkbox">
            </li>
            <% } %>
    </ul>
</h3>