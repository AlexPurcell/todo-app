</body>

</html>
<h3>

    <ul>
        <% for(var todos of items) { %>
            <li>
                <%= todos.title %> <input type="checkbox" id="cb">
            </li>
            <% } %>
    </ul>
</h3>